import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class DumbyLaser here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class DumbyLaser extends Bullet
{
    /**
     * Act - do whatever the DumbyLaser wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act() 
    {
        return;
    } 
    
    public DumbyLaser(double deltaX, double deltaY) {
        super(deltaX, deltaY);
    }
}
